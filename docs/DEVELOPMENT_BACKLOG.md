# 开发待办清单（校对版 · 2026-09-11）

- 校对日期：2026-09-11
- 用途：只保留“仍未完成 / 仍有效 / 已批准待实施”的项；已完成项移入第五节归档摘要。
- 证据基准：`master` @ `836b8b1`（基准 commit 于 2026-09-22 复核，条目内容仍为 2026-09-11 校对）；工作树另有 3 个未跟踪的本地临时脚本 `work/*.mjs`（`work/audit-endpoint-coverage.mjs`、`work/make-demo-shots.mjs`、`work/point-evidence-to-demo.mjs`），历史切片证据回查 git log `cce1ca0..3692450`。
- 范围决策备注：C5 采用 Redis 共享限流 + DB Outbox（事务内写入/退避/死信/重放）覆盖队列语义，按 2026-09-07 记录**不引入 BullMQ**；weapp 开发继续暂缓（等主程序完成后处理）。

状态图例：

- `[ ]` 未开始
- `[~]` 已批准/进行中

---

## 一、业务缺口（待继续开发）

### 1. 商家端 preview / planned 模块落地（2026-09-10 导航 IA 引入）

- [ ] 15 个 `preview` 模块：抢单大厅、评价管理、排班管理、标签分组、价格方案、分成方案、优惠活动、邀请奖励等，当前只在功能预览面板显示说明，不调用业务接口。
- [ ] 19 个 `planned` 模块：关系管理、员工管理、附加服务、限购方案、技能证书、店铺装修、会员成长、权益礼包、用户背包、互动玩法、提现管理、客户关怀、流失预警、私单风险、异常监测等，当前只登记名称与建设范围。
- 完成标志：任一模块从 preview/planned 转 `active` 时必须同时具备后端权限、API、页面状态与测试，不允许只改前端状态。

### 2. 收入账本与经营指标（经营工作台 v1 遗留）

- [ ] 经营工作台金额位“经营入账（应收/实收/毛利）待开通”：需先定义收入分录（订单客户实收、陪玩成本、门店毛利、平台费）与对应迁移/领域事件，再把首页占位升级为真实数字；禁止用订单价快照或陪玩应付冒充收入。
- [ ] 客户流失/沉默预警：基于客户档案与历史订单/场次统计最近下单时间、频次与金额变化，生成 `CUSTOMER_AT_RISK` 风险项；口径与阈值需单独设计，不进 v1 首页。

### 3. 登录与支付

- [x] **陪玩端端上下文切换接线 + `switch-context` 错误语义修复（2026-09-26，已修并验证，未提交）**：多角色账号（`roles=["CUSTOMER","PLAYER"]`）登录后落老板端（ADR-0009 决定 7 的端上下文单值判别），进入陪玩端前必须显式 `POST /api/v1/auth/switch-context`——移动端此前从未调用，表现为「商家已批准、账号仍进不去陪玩端、提示需要申请陪玩」。新增 `identityAdapter.switchContext`（契约 + H5 实现 + weapp 未配置桩）与 `features/player-context/{enter.ts,actions.ts}`，6 个陪玩端页面入口改为「先切上下文再加载」；403 → 「陪玩申请审核中」并保留会话，401 → 清 token 退回登录卡，切来的 token 不回写 session。同轮修掉 API 侧缺陷：`switch-context` 对「账号无目标端角色」返回 500（域错误未映射），改为 403；账号禁用/门店停用/账号不存在 → 401。运行时证据见 `docs/unverified-and-deferred.md` 第五节，残留登记见其 §C。
- [ ] 微信一键登录（手机验证码登录/注册 P1 已于 2026-09-10 完成，见第五节）。
- [x] **账号密码自助注册（SP2，2026-09-26 实施完成并提交，`pushed`，分支 `feat/account-password-registration`，PR #4，已推送）**：老板（`CUSTOMER`）与陪玩（`PLAYER`）自助注册——注册只建 `CUSTOMER`，`PLAYER` 仅经老板审批叠加；手机号可选绑定，绑定后手机号快捷登录与密码登录收敛到同一账号；存量随机密码账号走「设置密码」入口激活（初次设置免验原密码，修改必验）。新增 2 个 operation（`POST /api/v1/auth/register`、`POST /api/v1/auth/password`），**一次新增列迁移**（`tenant_accounts.password_set_by_user`）已获授权并执行（`20260926095348_add_password_set_by_user`，两库 44 migrations 核对通过，回滚=`DROP COLUMN`）。门禁全绿：`pnpm test` / `test:integration` / `test:tenant-isolation` / `typecheck` / `openapi:generate`（幂等）均退出码 0。设计：`docs/superpowers/specs/2026-09-26-account-password-registration-design.md` + `docs/adr/0009-account-password-registration-and-multi-role-authz.md`；实施：`docs/superpowers/plans/2026-09-26-account-password-registration-sp2.md`。前置 **SP1 多角色授权解析修复已于 2026-09-26 实施完成并提交**（`pushed`，同一分支，见 `docs/unverified-and-deferred.md` 第五节）。**遗留（均记录在 spec §12）**：陪玩端入口显隐未接线；`phone-login` 对停用门店的既有 500 缺陷未修；改密后不撤销其他 refresh 会话。
- [ ] 线上支付：钱包充值仍走本地模拟支付；接真实渠道前保持“可插拔支付模块”，实施需商户号/密钥（挂起）。
- [ ] 自动催缴：余额/可服务时长接近耗尽时通知老板续费（后端需定时检查 + 通知）。

### 4. 平台端剩余

- [ ] 开店表单保存草稿（当前提交即创建，无草稿态）。
- [ ] 平台总览缺失指标的兜底口径：存储用量等无计费数据字段当前返回 `unavailable`，接入对象存储计量后再补。

### 5. 页面与体验补全

- [ ] admin 旧页面（`app/(tenant)/orders|sessions|settlements|disputes|...`）与 `merchant-console` 新控制台并行；G4 渐进迁移继续“改到即迁”，旧的顶层页面在功能对齐后收敛或下线。
- [ ] mobile：H5 首页域名定位打通（本地无 `tenant_domains` 映射，随 H5 真实域名部署一起验证）。
- [ ] **大区 / 目标段位 / 模式的联动与联想（Bug 2，ADR-0010 已批准；第一批已实施，第二批未授权）**：下单时「目标段位」既不校验也不联想，用户要求区、段位、模式为**必填**并带下拉或辅助搜索（输入「翡翠」筛出翡翠 1-4，输入「翡1」→ 翡翠 1），预设由**平台端**表格预设库维护但**不限制老板自填**（填了库外值不应只报「没有对应数据」而卡住提交）。方案 = `docs/adr/0010-order-region-rank-mode-presets.md`（2026-09-27 **已批准**）；三处方案分歧点已由用户确认（按基础价 + 下单页提示 / 必填卡模板发布口不追溯存量 / **分两批实施**）。第一批（零迁移、不建表，只解「库外值不能提交」）实施计划 = `docs/superpowers/plans/2026-09-27-order-field-free-input-batch1.md`（**2026-09-27 已实施**，见下方条目）；第二批（平台预设库 / 建表迁移 / 检索端点 / 平台端维护页 / 候选列表控件 / 发布口必填 / 设计器放开三个语义角色）待第一批落地后另立计划。
  - 调研结论（2026-09-27，只读核实，全部带 file:line 证据，详见 ADR 背景）：① 老板端有 **v1 经典下单 / v2 通用模板两条并行链路**且 v2 不可用时**自动回退** v1（`game-order/index.tsx:619-620`），任一未改老板都会再撞同一问题；② 三字段**不是固定字段**，是靠 `semanticRole`（`SERVER_REGION`/`TARGET_RANK`/`MODE`）标识的模板组件，模板里没有该字段时下单链路完全不感知（`game-template-document.ts:270-303`）⇒「必填」目前不是任何一层的约束；③ 现场根因是 v1 老板端把 `select` 字段**降级成自由文本 `<Input>`**、选项只当 placeholder（`game-order/index.tsx:1049-1061`，`:1054`），**没有下拉也没有联想**；④ 库外值被**三层同时拦**：客户端不发请求（`order-values.ts:242-252`/`:335-407` + `:708-712`）、v1 返回 **400**（`game-template-values.ts:71-77`）、v2 返回 **422**；⑤ **平台端零基础**——`app/(platform)` 整目录对 rank/region/mode/template 零命中，导航仅 8 项，`GameRegion` 属 catalog 且与下单字段无外键、`GameDispatchRankRule` 是模板级 legacy ⇒ 做「平台预设库」必须**新建表 + 新接口 + 新页面 + 一次建表迁移**；⑥ v2 模板设计器**故意不暴露**这三个语义角色（`template-editor-meta.ts:28`/`:53-60`，依据设计规格 v0.2 的 D-13「只给原语，不给预设」）⇒ 商家在 UI 上无法标注"目标段位"，需先解该裁定边界；⑦ 无任何用例覆盖前缀联想、"三字段必填"、库外值不阻塞提交。
  - [x] **第一批（零迁移 / 不建表 / 不加端点 / 不改契约）2026-09-27 已实施并验证**：服务端 v2 单选豁免（`game-template-values.ts` / `game-template-calculations.ts` / `game-template-document.ts`，多选与表格列**不**豁免）、老板端 v2 本地预检豁免与「预设按钮 + 自由文本输入」混合控件（`order-values.ts` / `game-order/index.tsx`）、v2 集成回归 1 条与 API / mobile 单测 4 条；3 条既有断言按「覆盖改挂到未豁免的 `CUSTOM` 字段」收窄（无删除）。全套门禁退出码 0：`pnpm test` 953 passed / 1 skipped、`test:integration` 315、`test:tenant-isolation` 44、`typecheck`、`build:h5`、`build:weapp`。**证据缺口：只有自动化证据、无页面人工实测**（按用户授权「页面人工实测另起服务时再单独报」）。实施记录、UI 偏差与未决项见 ADR-0010「实施记录（第一批）」；三处新登记缺口见 `docs/unverified-and-deferred.md` §C（v1 语义角色不可达 ⇒ v1 经典链路仍拦库外值 / 移动端表格列不豁免 / admin-web 预览第三份实现）。
  - [ ] **第二批（未授权，需另立计划）**：平台预设库 `platform_game_presets` + 一次建表迁移、租户侧只读检索 `GET /api/v1/tenant/game-dispatch/presets`、平台端维护页 + 导航项、候选列表控件（`Input` + 自绘候选，H5 / weapp 同源，含 `aliases` 前缀/包含匹配）、模板发布口「必需语义角色」校验（决定 7，不追溯存量）、设计器放开三个语义角色（决定 9，需先修订 D-13）。**依赖**：迁移与契约变更按 AGENTS.md 均需**单独授权**；第二批需先解掉第一批登记的 v1 语义角色可达性缺口（否则决定一「两条链路都要改」只在 v2 成立）。
- [ ] **Taro H5 禁用态按钮**：`<Button disabled>` 的禁用样式在 H5 下会盖掉按钮文字（Bug 1 的「按钮没有名称」即此根因），本轮只在 `customer-ui` / `player-ui` 补了 `[disabled]` 覆盖，其他页面新增禁用按钮需自查；`pw-button[disabled]{opacity: .48}` 的对比度也未统一调整。

### 6. 算价模型（待立项，2026-09-20 已确认口径）

- [ ] 口径（不得回退）：单价 = **陪玩×游戏底价** + **按游戏绑定的加价规则**；时长由陪玩**结束服务时报单**填写（申报时长为计费依据，开始/结束截图证据用于人工核查）；报名界面显示**单价**（不乘时长，老板与陪玩同一数字）；陪玩实收在报单后显示；平台费置 0，`门店毛利 = 订单金额 − 陪玩实收`。
- [ ] 归属报名/结算模块；派单模板只负责「客服填什么」，**不管钱**。
- [ ] 后续项一：固定价（FIXED）独立立项（由客服在派单时填写，老板不参与）。
- [ ] 后续项二：时长智能核验（申报时长与截图证据计时自动比对并标记）；本版只保存截图供客服审批时人工核查。
- 设计已落库：`docs/specs/算价模型-设计规格-v0.1.md` + `docs/adr/0003-order-pricing-model.md`（架构决策待批准）。
- 决策沿革：原快照 `work/session-state-2026-09-18.md`（提交 `8e7067e`）的旧口径「单价由陪玩报名时填 / 总价逐条累加」已被 2026-09-20 口径取代。

## 二、工程与质量收尾

- [x] 工作树改动提交（2026-09-20 由 PR #3 完成）：商家端导航 IA 与 UI 打磨、陪玩端 order-hall UI、E2E 更新与演示种子调整均已入库。
- [ ] E2E 已正式入库并替换根脚本 `test:e2e`（`playwright test`，2026-09-10 最近一次运行 `passed`）；仍待把 `test:e2e` 接入 CI（当前 CI 无浏览器与服务启动步骤）。2026-09-22 决策：**暂不接 CI**——它需要 3005 与 3101 两个服务、Edge 通道和走查夹具，且视觉基线只有 win32，接 Linux CI 会全红、须先重做基线；CI 里改为加一条 `playwright test --list` 的解析门禁，挡住 spec 被改坏却无人发现。
- [ ] F4 容量基线（100 租户 / 100 万订单，只读 100 RPS、写 50 RPS、P95 达标记录）。
- [ ] 多时区：报表「今日」的日界当前写死 `Asia/Shanghai`（2026-09-22 核对三个库，`tenants.timezone` 全部是 `Asia/Shanghai`，与常量一致，故未改成读配置）；真正支持多时区需先做产品决策（按租户切日界、跨端时区展示口径）。
- [ ] 覆盖率质量债（已延后，2026-09-08）：`test:coverage` 已合并单元+集成，当时 branch 63.7%（目标 80%，critical-domain 已达 100/100/100/100）；待 E2E/支付/部署等优先项推进后再补。**注意：`tests/vitest.coverage.config.ts` 的 branch 阈值目前是 60，与这里的 80 目标不一致——直接把阈值提到 80 会让 CI 变红，应先补覆盖再提阈值；本轮未复跑覆盖率，故不在此写当前百分比。**（2026-09-26 补充）前述「critical-domain 已达 100/100/100/100」是 2026-09-08 的快照，**已失效过一次**：业务资金线 `ba2d12e` 新增 `fenToYuanText` 未配套关键域用例，关键域门禁自 2026-09-26 早上起在 master 上红（lines 92.18% / functions 80%），并因 Actions 首个失败步即止而连累其后 7 步（含 `format:check` 与 build 三连）被 skip；`5f33b19` 补齐用例后恢复 100%，`30acbb4`（prettier 基线）后本分支 CI 全绿。详见 `docs/unverified-and-deferred.md` 第五节。**
- [ ] 可选：worker 崩溃注入测试与死信人工管理界面（outbox relay 已有租约回收/退避/死信/人工重放）。
- [ ] 门禁环境依赖：集成测试与 E2E 需要本地 Docker（postgres 5433 / redis 6380 / minio）；Docker 未启动时 `/health` 用例会因 `database=down` 失败，属环境问题而非代码回归。
- [ ] **契约缺口：错误响应码不在 OpenAPI**（2026-09-26 登记）：`POST /api/v1/auth/switch-context` 的 400/401/403 只存在于代码与集成测试里——该控制器方法没有 `@ApiResponse` 装饰器，`pnpm openapi:check` 对此零覆盖（本轮 `openapi:generate` 生成物零 diff 即是证据）。同类端点若都缺注解，契约层看不到任何错误语义；补法：逐端点补错误响应注解后重新生成并提交生成物。
- [ ] **移动端 E2E 缺多角色夹具**（2026-09-26 登记）：`tests/e2e/mobile.spec.ts` 只用纯 `player` 账号，「多角色账号进陪玩端必须先切端上下文」这条修复路径没有 E2E 覆盖（现有证据为集成测试 + 手工 CDP 取证）。

## 三、发布与运维（阶段 H）

- [ ] H1 Linux 容器化：`infra/docker` 的 Dockerfile（api/admin/h5）与 `docker-compose.prod.yml` 已就绪，待真实 Linux 主机实机验证。
- [ ] H2 备份/恢复/RPO/RTO 演练与 runbook（deploy/rollback/restore）。
- [ ] H3 phase-1 acceptance 文档与四条关键路径 E2E 结论。
- [ ] H4 审计/日志脱敏上线前复查（代码层 C2 已结，需运行时日志与平台跨租户授权复查）。
- [ ] H5 真实域名部署验证（H5 品牌运行态 + 同源反代）。
- [ ] H6 统一访问入口 + 设备自动跳转：手机/微信内 → H5 角色端，电脑 → 管理后台，并确定默认落地页与租户/门店定位方式（2026-09-07 用户提出）。
- [ ] 生产 `PII_MASTER_KEY` 配置与备份（开发环境用 `SESSION_SECRET` 派生，生产强制独立主密钥）。

## 四、外部依赖挂起（等账号/授权/条件）

| 项                                               | 解除条件                                                                  |
| ------------------------------------------------ | ------------------------------------------------------------------------- |
| 微信小程序真机/审核/发布                         | 提供微信 AppID + 开发者工具 + 审核/发布授权（用户已决定主程序完成后处理） |
| 微信一键登录                                     | 提供微信开放平台 AppID/AppSecret 与回调域名                               |
| 真实支付（钱包充值渠道）                         | 商户号/密钥；当前为本地模拟支付                                           |
| 短信通道 / 微信通知 / AI Provider / 生产对象存储 | 到达对应切片并提供密钥/账号                                               |
| mobile React 19 回归                             | Taro 上游支持 react@19 后独立升级任务                                     |
| 真实域名/服务器/反向代理                         | 用户提供后进入 H5/H6 部署验证                                             |

## 五、本次校对移除的已完成范围（归档摘要）

- 2026-09-11 校对移除：T1 平台开店闭环（含一键开店自动创建店主账号与 H5 交付）、P-B1 平台总览/订阅/门店详情、P-B2a 平台账号管理、P-B2b 临时跨租户授权、P-B3 平台级汇总审计与导出、平台费率调整入口、会话收敛（`/auth/logout` + `/auth/refresh`）、订阅续费接口与页面、经营工作台 v1（后端 `reporting` + `dashboard/summary` + 前端 WorkPage）、商家端控制台 P0–P4 与场次/证据/结算/争议/审计/通知/客户陪玩详情页面、手机号验证码注册登录 P1、陪玩入驻申请、四端 UI 模板库、生产打包起步（Dockerfile + prod compose + runbook）。
- 2026-09-17 校对移除：通用派单模板 epic 的 S1b/S2/S3/S4 全部完成（S1b 兼容转换、S2 模板管理 API 12 条 operation、S3 正式模板管理界面、S4 多游戏新建派单与订单快照），作者范围外仅剩 S5 灰度/观测/兼容收口。
- 2026-09-07 校对时移除：A1–A5（越权/会话/CSRF/运行隔离/手机号加密）、B1–B5（订单/场次/金额/幂等/账本）、C1–C5（审计/通知/Outbox/worker/Redis 限流）、D1–D3（功能门禁/开通生命周期）、E1–E5 已达标的工程项、F1 根命令与 F5 证据负例、G1/G2 主链路页面、G3 平台适配收敛、R5 双端登录/订单主链路页面、Block1 与 Block2 P1–P5。
- 若需逐条证据，请回查 git log `cce1ca0..3692450`（基线）与 `6935897..97bcc4c`（2026-09-08 ~ 2026-09-10 增量）；两份文档的归档摘要只用于快速追溯。
