# 开发待办清单（校对版 · 2026-09-07）

- 校对日期：2026-09-07
- 用途：只保留“仍未完成 / 仍有效 / 已批准待实施”的项；已完成的切片已从本清单移除。
- 完成证据：见 git log `cce1ca0..3692450`（Slice 0–11、R1–R5、Block1、Block2 P1–P5）；`docs/unverified-and-deferred.md` 与本文件已同步为“仅剩开放项 + 归档摘要”口径，不再重复历史明细。
- 范围决策备注：C5 采用 Redis 共享限流 + DB Outbox（事务内写入/退避/死信/重放）覆盖队列语义，按 2026-09-07 记录**不引入 BullMQ**；weapp 开发继续暂缓（等主程序完成后处理）。

状态图例：

- `[ ]` 未开始
- `[~]` 已批准/进行中

---

## 一、业务缺口（待继续开发）

### 1. 平台开店闭环（联调 T1，待修）

- [ ] 描述：平台 `/tenants` 新建门店只创建租户壳，未自动创建店主账号；`/platform/onboarding/tenants`（owner + 订阅 + 品牌）后端已存在但平台端没有前端入口。
- 完成标志：平台一键开店后自动得到可登录的店主账号；验收用例覆盖“开店 → 店主登录 → 进入商家端”。

### 2. 老板/陪玩登录与支付（当前为本地可跑形态）

- [ ] 微信一键登录 / 手机验证码登录：老板注册账号后自动生成老板编号（现状为账号密码登录）。
- [ ] 线上支付：钱包充值目前走本地模拟支付；接真实渠道前保持“可插拔支付模块”，实施需商户号/密钥（挂起）。
- [ ] 自动催缴：余额/可服务时长接近耗尽时通知老板续费（后端需定时检查 + 通知）。

### 3. 页面与体验补全

- [ ] admin：结算批次管理页（结算批次状态机后端已有，`/finance` 目前只有费率与试算）。
- [ ] admin：旧订单页筛选与独立详情页完善（当前详情在同一页内展开）。
- [ ] admin：场次/证据管理独立视图（当前在订单/派单操作内联）。
- [ ] mobile：收入明细、争议详情独立页（当前在资料/订单列表内联）。
- [ ] mobile：H5 首页域名定位打通（本地无 `tenant_domains` 映射，随 H5 真实域名部署一起验证）。
- [ ] G4 渐进迁移剩余：admin 旧页面继续“改到即迁”至 Tailwind v4 + shadcn/ui + TanStack Query（新页面已是新栈）。

## 二、工程与质量收尾

- [ ] F2/F3：正式 Playwright E2E 入库并接入 CI `test:e2e`（09-06 已手工跑通关键路径，但根脚本 `test:e2e` 仍是 `not-implemented` 占位；CI 已跑 integration/isolation/coverage/format/openapi）。
- [ ] F4：容量基线（100 租户 / 100 万订单，只读 100 RPS、写 50 RPS、P95 达标记录）。
- [ ] 覆盖率质量债：integration 全局 branches 门槛由 60 提升到 80（critical-domain 已达 100/100/100/100）。
- [ ] 可选：worker 崩溃注入测试与死信人工管理界面（outbox relay 已有租约回收/退避/死信/人工重放）。

## 三、发布与运维（阶段 H）

- [ ] H1 Linux 容器化 + API/worker/admin Dockerfile 验证
- [ ] H2 备份/恢复/RPO/RTO 演练与 runbook（deploy/rollback/restore）
- [ ] H3 phase-1 acceptance 文档与四条关键路径 E2E 结论
- [ ] H4 审计/日志脱敏上线前复查（代码层 C2 已结，需运行时日志与平台跨租户授权复查）
- [ ] H5 真实域名部署验证（H5 品牌运行态 + 同源反代）
- [ ] H6 统一访问入口 + 设备自动跳转：正式域名部署后提供一个总入口，根据访问设备自动引导——手机/微信内 → H5 角色端（陪玩/老板），电脑 → 管理后台或桌面版页面；需一并确定默认落地页与租户/门店定位方式（2026-09-07 用户提出）。
- [ ] 生产 `PII_MASTER_KEY` 配置与备份（开发环境用 `SESSION_SECRET` 派生，生产强制独立主密钥）。

## 四、外部依赖挂起（等账号/授权/条件）

| 项 | 解除条件 |
| --- | --- |
| 微信小程序真机/审核/发布 | 提供微信 AppID + 开发者工具 + 审核/发布授权（用户已决定主程序完成后处理） |
| mobile React 19 回归 | Taro 上游支持 react@19 后独立升级任务 |
| 短信/微信通知、真实支付、AI Provider、生产对象存储 | 到达对应切片并提供密钥/账号 |
| 真实域名/服务器/反向代理 | 用户提供后进入 H5/H6 部署验证 |

## 五、本次校对移除的已完成范围（归档摘要）

- 2026-09-07 校对时移除：A1–A5（越权/会话/CSRF/运行隔离/手机号加密）、B1–B5（订单/场次/金额/幂等/账本）、C1–C5（审计/通知/Outbox/worker/Redis 限流）、D1–D3（功能门禁/开通生命周期）、E1–E5 已达标的工程项、F1 根命令与 F5 证据负例、G1/G2 主链路页面、G3 平台适配收敛、R5 双端登录/订单主链路页面、Block1（通用订单+派单+选人）与 Block2 P1–P5（老板钱包/场次结算）。
- 若需逐条证据，请回查 git log `cce1ca0..3692450`；两份文档的归档摘要只用于快速追溯。
