# 台账（校对版 · 2026-09-07）

- 用途：记录当前“未验证 / 未完成 / 仍挂起”的项与解除条件。
- 校对日期：2026-09-07；与 `docs/DEVELOPMENT_BACKLOG.md`（校对版）使用同一状态口径。
- 归档说明：历史切片明细（Slice 0–11、R1–R5、Block1、Block2 P1–P5 及联调修复 T1/T2 相关过程）已完成或已归入待办，本文件不再逐日保留“完成历史”；逐条证据回查 git log `cce1ca0..3692450`。
- 范围决策备注：Redis 共享限流 + DB Outbox 已覆盖队列语义，按 2026-09-07 记录不引入 BullMQ；weapp 开发暂缓（等主程序完成后处理）。

## A. 需第三方 / 账号 / 授权（仍挂起）

| 项 | 状态日期 | 解除条件 |
| --- | --- | --- |
| 微信小程序 weapp 真机/审核/发布 | 2026-09-07 | 提供微信 AppID + 开发者工具 + 审核/发布授权；用户已决定主程序完成后处理 |
| mobile React 19 回归（现 React 18.3.1） | 2026-09-07 | Taro 上游支持 react@19 后独立升级任务 |
| 短信/微信通知、真实支付、AI Provider、生产对象存储 | 2026-09-07 | 到达对应切片并提供密钥/账号 |
| 真实域名/服务器/反向代理 | 2026-09-07 | 用户提供后进入 H5/H6 部署验证 |
| 生产 `PII_MASTER_KEY` | 2026-09-07 | 部署时生成并备份（开发环境用 `SESSION_SECRET` 派生，生产强制独立主密钥） |
| 微信一键登录 / 手机验证码登录（老板注册自动编号） | 2026-09-07 | 提供微信 AppID / 短信服务商账号；当前为账号密码登录 |
| 线上支付（钱包充值真实渠道） | 2026-09-07 | 商户号/密钥；当前为本地模拟支付，先保持“可插拔支付模块” |

## B. 需真实运行环境 / 发布阶段（仍挂起）

| 项 | 状态日期 | 解除条件 |
| --- | --- | --- |
| H5 品牌运行态 + 同源反代/公网部署（含 H5 首页域名定位） | 2026-09-07 | 真实域名并部署；本地无 `tenant_domains` 映射 |
| admin/mobile 运行态 E2E | 2026-09-07 | 运行中的 API + 真实域名；本地已按构建级验收 |
| Linux 容器化 + API/worker/admin Dockerfile 验证 | 2026-09-07 | Slice 12 / 发布阶段 |
| 备份/恢复/RPO/RTO 演练与 runbook | 2026-09-07 | 发布阶段 |
| phase-1 acceptance 文档与四条关键路径 E2E 结论 | 2026-09-07 | E2E 正式入库后执行 |
| 统一访问入口 + 设备自动跳转（H6） | 2026-09-07 | 正式域名部署期实现：手机/微信内 → H5 角色端，电脑 → 管理后台/桌面版 |

## C. 工程与体验收尾（本地可继续开发）

- 平台开店闭环（联调 T1）：平台 `/tenants` 新建门店未自动创建店主账号；`/platform/onboarding/tenants` 后端已有但平台端缺前端入口。
- admin：结算批次管理页（后端批次状态机已有，`/finance` 目前只有费率与试算）。
- admin：旧订单页筛选与独立详情页完善；场次/证据管理独立视图。
- mobile：收入明细、争议详情独立页（当前内联展示）。
- G4：admin 旧页面“改到即迁”至 Tailwind v4 + shadcn/ui + TanStack Query。
- Playwright E2E 正式入库并接入 CI `test:e2e`（根脚本现为 `not-implemented` 占位；CI 已跑 integration/isolation/coverage/format/openapi）。
- 容量基线（100 租户 / 100 万订单，只读 100 RPS、写 50 RPS、P95 达标记录）。
- 覆盖率质量债：integration 全局 branches 门槛 60 → 80（critical-domain 已达 100/100/100/100）。
- 自动催缴：余额/可服务时长接近耗尽时通知老板续费。
- 可选：worker 崩溃注入测试与死信人工管理界面（outbox relay 已有租约回收/退避/死信/人工重放）。

## D. 已完成并归档（摘要，不再展开）

- 2026-09-07 校对：Slice 0–11、R1–R5、Block1（通用订单+派单+选人）、Block2 P1–P5（老板钱包/场次结算）及本地联调修复 T2 均已结项；A1–A5、B1–B5、C1–C5、D1–D3、E1–E5 达标项、F1/F5、G1/G2 主链路、G3 已完成。
- 原“多实例登录限流 / CSRF / CI integration / 平台独立凭据与审计”等旧挂账项已在 2026-09-07 前结清，不再列出。
