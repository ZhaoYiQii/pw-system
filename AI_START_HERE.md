# AI 从这里开始

这是一个正在开发的多租户 SaaS 单仓。长期规则只看 `AGENTS.md`；本文件只负责把任务路由到最少必要上下文。

## 默认读取集合

1. `AGENTS.md`
2. 本文件
3. 用户点名的文件或当前任务的目标文件

不要默认完整读取 `PROJECT_STATUS.md`、主规格、历史计划、验收文档、OpenAPI 生成物或整个 `docs/`。需要某项事实时按下面路由读取对应章节。

## 任务路由

| 任务                             | 追加读取                                                                   |
| -------------------------------- | -------------------------------------------------------------------------- |
| 当前进度、开放项                 | `PROJECT_STATUS.md` 的相关小节；实时状态以 `git status --short` 和代码为准 |
| 产品范围、租户/金额/状态机不变量 | 主规格的相关章节，不读整份；章节索引见文件目录                             |
| 当前功能实现                     | 当前 Slice 计划 + 目标代码 + 最近验收记录                                  |
| 数据库/RLS                       | 相关 migration、repository、隔离测试和对应 ADR                             |
| API 契约                         | controller/schema、目标 operationId；禁止全文输出 OpenAPI/生成客户端       |
| 前端 UI                          | 当前页面、邻近组件、UI 规格相关章节；不默认读取所有设计稿                  |
| 发布/部署                        | 对应 runbook、环境清单和当次明确授权                                       |

## 当前工作口径

- 平台端、商家端、陪玩端和老板端主链路已有本地实现；当前代码和迁移优先于历史文档。
- 通用派单模板 S1b、S2、S3、S4 已有本地验收记录；剩余工作以当前 S5 计划和代码事实为准。
- 工作树长期存在用户改动。禁止 reset/checkout、批量格式化或无路径完整 diff；只处理当前任务拥有的文件。
- 移动端当前是 Taro 4 + React 18.3.1；异步可靠性采用 Redis 共享限流 + 数据库 Outbox，不引入 BullMQ。
- Docker 未就绪时，依赖 PostgreSQL/Redis/MinIO 的集成测试和 E2E 只能标记未验证。

## 执行方式

有明确授权的有界任务可直接进入单一 Slice；只在存在范围不清、外部副作用或权限门禁时暂停。优先聚焦测试与路径限定检查，完成后报告：实际修改文件、验证命令及退出码、未验证项、所用 skill。不得自动进入下一 Slice，也不得把本地修改描述成已提交、已推送或已部署。
