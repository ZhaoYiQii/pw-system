# AI 从这里开始

本仓库当前是“项目治理初始化”状态，不是已经生成的应用。不得因为目录为空就自行选择框架或补写业务代码。

## 必读顺序

1. `AGENTS.md`
2. `PROJECT_STATUS.md`
3. `docs/specs/陪玩门店多租户SaaS-H5-开发主规格-v1.0.md`
4. `docs/adr/README.md`
5. 当前任务明确指定的其他文件

## 第一次响应必须做什么

只进行只读预检，并向用户报告：

1. 你理解的第一期范围和非目标；
2. 当前目录、Git、Node、pnpm、Docker 与 WSL2 状态；
3. 已存在和缺失的文件、命令与能力；
4. 主规格与真实仓库是否冲突；
5. 建议执行的唯一 Slice；
6. 预计创建或修改的准确文件；
7. 需要安装的准确依赖与版本解析方法；
8. 测试、双端构建和 Linux 容器验证计划；
9. 需要用户单独授权的动作；
10. 无法从证据确定的问题。

完成预检后停止。用户没有批准具体 Slice 和权限动作前，不得开发。

## 执行一个 Slice

获得批准后仍然只能执行被点名的 Slice：

```text
读取规格与现状
→ 给出准确计划
→ 等待必要权限
→ 建立失败测试或替代证据
→ 最小实现
→ 运行切片测试
→ 运行租户/契约/双端相关门禁
→ 报告实际证据
→ 停止，等待下一 Slice
```

交付报告必须采用主规格第 21 章模板。

## 当前允许的结论

当前最多只能说“治理文件与规格已初始化”。没有依赖、代码、测试、构建产物、Git 提交或部署证据，不能声称项目已搭建、可运行或可部署。
