/**
 * 商家端控制台的路径前缀。
 *
 * 侧栏 href 与面包屑解析都用这一个常量：`findActiveNavChild` 拿它拼出候选 href
 * 再与注册表里的 href 做字符串相等比较，前缀一旦分叉就会**静默失配**
 * （面包屑降级成模块名、不报错），是最难发现的一类回归。
 *
 * 范围说明：这目前是「侧栏 + 面包屑」这一子系统的唯一来源，不是全站唯一来源——
 * 各页还有约 60 处深链写着自己的完整路径（`work-view.tsx`、`order-detail.tsx` 等），
 * 它们指向具体路由目标、不参与前缀拼接比对，尚未迁移。
 *
 * 刻意做成零依赖的叶子文件：`dispatch-view.tsx` 之类的页面只需要前缀，
 * 不该为此引入整个导航注册表。
 */
export const CONSOLE_BASE = "/merchant-console";
